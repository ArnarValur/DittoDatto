name: media_manager
description: Shared media management package for DittoDatto Flutter apps — gallery, picker, storage abstraction, SurrealDB CRUD.
publish_to: 'none'
version: 0.1.0

resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  ditto_auth:
    path: ../ditto_auth
  ditto_design:
    path: ../ditto_design
  file_picker: ^9.2.1
  flutter_riverpod: ^3.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

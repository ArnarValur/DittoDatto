name: ditto_auth
description: Shared authentication package for DittoDatto Flutter apps — SurrealDB-native auth with swappable backend.
publish_to: 'none'
version: 0.1.0

resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  surrealdb: ^1.1.2
  flutter_secure_storage: ^10.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

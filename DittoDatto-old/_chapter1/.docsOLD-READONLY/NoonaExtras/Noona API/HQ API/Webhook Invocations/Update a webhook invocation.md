---
tag: "noona.is"
---
Updates a webhook invocation.

POST `/v1/hq/webhook_invocations/{webhook_invocation_id}`

Authorization Bearer <token>

The Noona HQ API uses Bearer token for authentication.

Endpoints that return generic information that does not contain sensitive data do not require authentication. Endpoints that require authentication are specifically marked in the documentation.

**Authorization: Bearer your-token**

In: `header`

## Path Parameters

webhook\_invocation\_id \* string

Webhook Invocation ID

Example `"dwawd8awudawd"`

## Query Parameters

## Request Body

application/json

## Response Body[List all webhooks invocations GET](https://docs.noona.is/docs/hq/webhook-invocations/ListWebhookInvocations)

[

Previous Page

](https://docs.noona.is/docs/hq/webhook-invocations/ListWebhookInvocations)[

Webhooks

Next Page

](https://docs.noona.is/docs/hq/webhooks)
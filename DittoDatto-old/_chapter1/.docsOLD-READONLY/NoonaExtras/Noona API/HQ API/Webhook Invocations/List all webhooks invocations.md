---
tag: "noona.is"
---
Lists all webhook invocations of a company.

GET `/v1/hq/companies/{company_id}/webhook_invocations`

Authorization Bearer <token>

The Noona HQ API uses Bearer token for authentication.

Endpoints that return generic information that does not contain sensitive data do not require authentication. Endpoints that require authentication are specifically marked in the documentation.

**Authorization: Bearer your-token**

In: `header`

## Path Parameters

company\_id \* string

Example `"dwawd8awudawd"`

## Query Parameters

## Response Body[Get webhooks invocation GET](https://docs.noona.is/docs/hq/webhook-invocations/GetWebhookInvocation)

[

Previous Page

](https://docs.noona.is/docs/hq/webhook-invocations/GetWebhookInvocation)[

Update a webhook invocation POST

Next Page

](https://docs.noona.is/docs/hq/webhook-invocations/UpdateWebhookInvocation)
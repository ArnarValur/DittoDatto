# 📦 Pulse Archive — February 2026 (Part 1)

> Archived from `pulse.md` on 2026-02-15 to keep the active pulse under 150 lines.
> These are historical session logs. The active pulse only keeps the last 2 sessions.

---

## ✅ Completed Tracks (archived 2026-02-19)

| Domain             | Track                          | Completed  | Notes                                                         |
| ------------------ | ------------------------------ | ---------- | ------------------------------------------------------------- |
| mercury-engine     | **Booking Engine Fix**         | 2026-02-19 | `undefined` → Firestore crash fixed + expired hold GC         |
| public-marketplace | **Faker Seed + Emulator**      | 2026-02-16 | Full dataset seeded, 28 categories, icon mapping resolved     |
| all-apps           | **Auth Session Hardening**     | 2026-02-15 | Cookie bypass, CORS, emulator-to-live switch                  |
| public-marketplace | **Public Auth & Registration** | 2026-02-12 | Google + Phone + Email, live-tested                           |
| business-portal    | **Portal Auth & Dashboard**    | 2026-02-12 | Google login + company switching verified                     |
| admin-panel        | **User Management Wiring**     | 2026-02-12 | Custom claims set correctly for business users                |
| public-marketplace | **DittoDatto Theming**         | 2026-02-16 | SolarTheme, Moody Blue, component theming system              |
| public-marketplace | **Booking MVP Phase 4**        | 2026-02-10 | Hold countdown, error handling, confirm flow; cancel deferred |
| public-marketplace | **Profile Overview Redesign**  | 2026-02-10 | Hero card, calendar dots, cancel modal                        |
| public-marketplace | **Profile i18n + Chat**        | 2026-02-18 | Full i18n + user-facing booking chat UI                       |
| public-marketplace | **Footer Pages (404 Fix)**     | 2026-02-16 | 404 page + static footer routes                               |

---

## Archived Session Memory

### 2026-02-12 (Public Auth & Registration — Multi-Provider Login)

**What was accomplished:**

1. ✅ **`onAuthUserCreated` trigger fixed** — Rewrote `on-auth-create.ts` to create `users/{uid}` Firestore doc on EVERY signup (email, Google, phone) + staff auto-link + custom claims via `beforeUserCreated` response. Exported as `auth_onUserCreated` from `index.ts`.
2. ✅ **`useAuth.ts` enhanced** — Added `signInWithPhone()` (OTP via invisible reCAPTCHA), `verifyPhoneOTP()`, `cleanupRecaptcha()`, phone verification state tracking.
3. ✅ **`PhoneOTPForm.vue` created** — Two-step component: phone input → OTP code entry, resend timer (60s), change-number flow, auto-cleanup on unmount.
4. ✅ **`login.vue` redesigned** — Multi-provider view: Google / Phone / Email with back-navigation between views. BankID button commented out with PostIt.
5. ✅ **`signup.vue` redesigned** — Same multi-provider approach. Phone signup has name-collection step after OTP verification. BankID commented out.
6. ✅ **`auth.vue` layout upgraded** — Subtle gradient background (`bg-linear-to-br from-primary/5`), hover scale on logo.
7. ✅ **`useBankID.ts` scaffold** — Interface definitions (`BankIDVerificationResult`, `BankIDConfig`), stub methods returning "not available". Documented provider options (Signicat, Criipto, BankID Norge).
8. ✅ **Business Portal Google sign-in** — Added Google OAuth to portal login alongside email/password. Refactored duplicate `claimInvite` logic into `postLoginClaimFlow()` function.

**Build verification:** `npx tsc --noEmit` — only new warning is TS7030 on `on-auth-create.ts` (benign: `beforeUserCreated` accepts `void | BeforeCreateResponse`). All other errors pre-existing.

**Evening session (22:00–23:04): Switched to fully-live Firebase**

Discovered emulator connections were scattered across 3 layers:

1. `nuxt.config.ts` `vuefire.emulators` blocks (3 apps)
2. `.env.development` with `FIREBASE_AUTH_EMULATOR_HOST` (admin-panel)
3. Hardcoded `connectFunctionsEmulator()` calls in composables/components (6 files across 2 apps)
4. `emulator-debug.client.ts` manual plugin (admin-panel — user commented out)

All 12 emulator connection points disabled with `⚠️ EMULATORS OFF` comments for easy reversal.

Admin panel bootstrapped:

- Created new Firebase Auth user `arnarvalur@avj.info`
- Temporarily bypassed `admin-auth.global.ts` middleware to set `super_admin` role in live Firestore
- Middleware re-enabled after user doc created

**⚠️ NOTE:** All emulators are OFF. The apps are hitting **live production Firebase**. When returning to emulator dev, search for `⚠️ EMULATORS OFF` and uncomment all blocks.

**reCAPTCHA note:** User has reCAPTCHA Enterprise via GCP Organization (`6Lf4-mgs...`). Firebase's `RecaptchaVerifier` auto-detects Enterprise — no manual wiring needed.

**Key files created:**

- `apps/web/public-marketplace/app/components/auth/PhoneOTPForm.vue`
- `apps/web/public-marketplace/app/composables/useBankID.ts`

**Key files modified:**

- `packages/functions/src/staff/on-auth-create.ts` — rewritten (profile doc + staff link + claims)
- `packages/functions/src/index.ts` — exported `auth_onUserCreated`
- `apps/web/public-marketplace/app/composables/useAuth.ts` — phone auth methods
- `apps/web/public-marketplace/app/pages/login.vue` — multi-provider redesign
- `apps/web/public-marketplace/app/pages/signup.vue` — multi-provider redesign
- `apps/web/public-marketplace/app/layouts/auth.vue` — subtle gradient upgrade
- `apps/web/business-portal/app/pages/index.vue` — Google sign-in + refactored claim flow

---

### 2026-02-12 (Staff-Aware Booking Flow — Phase 5C: All 4 Layers)

**What was accomplished:**

1. ✅ **Layer 1: Engine Logic** — Created `staff-availability.ts` (pure functions: `isStaffAvailable`, `getAvailableStaffForSlot`, `fitsWithinBlocks`). Modified `data.ts` to fetch bookable staff in parallel. Modified `calculator.ts` with staff-aware slot filtering + backward-compatible fallback. Modified `index.ts` to accept optional `staffId`.
2. ✅ **Layer 2: Storefront Visibility** — Added `showOnStorefront: boolean` to `StaffMemberSchema`. Wired toggle into `StaffFormSlideover.vue` (only visible when `isBookable` is on, auto-resets to false).
3. ✅ **Layer 3: People Tab** — Created `StaffGrid.vue` (circular portraits, initials fallback, responsive grid, bookable dot). Modified `EstablishmentPage.vue` with "Our People" tab (appears only when staff data exists). Wired `[slug].vue` to fetch `showOnStorefront + isBookable + active` staff.
4. ✅ **Layer 4: Booking Flow** — Added `staffId` to `useBooking.ts` `FetchSlotsOptions`. Staff context banner in `BookingSlideover.vue` ("Booking with {name}" + clear button). `handleBookWithStaff` in `[slug].vue` pre-filters slots. `handleClearStaff` resets filter.

**Build verification:** `npx tsc --noEmit` passes — all errors are pre-existing Date/string mismatches.

**Critical finding during session:**

- `onAuthUserCreated` (`on-auth-create.ts`) exists but was **NOT exported from `index.ts`** — dead code → **FIXED in auth session above**.
- Staff invite creates Firestore shadow profile only — **no invitation email is sent**.
- Emulator-created Auth users don't get Firestore `users/` docs → **FIXED: trigger now creates profile doc**.

**Key files created:**

- `packages/functions/src/MercuryEngine/staff-availability.ts`
- `packages/mercury-engine/src/core/staff-availability.ts` (mirror)
- `packages/ui/components/establishment/StaffGrid.vue`

**Key files modified:**

- `packages/functions/src/MercuryEngine/data.ts` — staff fetch in parallel
- `packages/functions/src/MercuryEngine/calculator.ts` — staff-aware loop
- `packages/functions/src/MercuryEngine/index.ts` — staffId passthrough
- `packages/shared-types/src/staff.ts` — showOnStorefront field
- `apps/web/business-portal/app/components/staff/StaffFormSlideover.vue` — storefront toggle
- `packages/ui/components/establishment/EstablishmentPage.vue` — People tab
- `packages/ui/components/booking/BookingSlideover.vue` — staff context banner
- `apps/web/public-marketplace/app/pages/[category]/[slug].vue` — staff fetch + selection
- `apps/web/public-marketplace/app/composables/useBooking.ts` — staffId

---

### 2026-02-12 (Staff Management — Phase 3 Cloud Functions: Invite, Kill Switch, Rules)

**What was accomplished:**

1. ✅ **`staff_claimInvite` callable** — Existing users claim pending staff invites. Queries `collectionGroup('staff')` for matching email + `status: 'invited'`, sets `userId`, flips to `active`, merges `companyIds` into custom claims.
2. ✅ **`staff_onStatusChange` Firestore trigger** — Kill switch: when status → `removed`/`suspended`, strips company from custom claims. Restores on re-activation from `suspended`.
3. ✅ **Suicide prevention UI** — Owner gets crown banner (can't edit permissions/status/remove), self-edit lockout on `can_manage_staff` with warning badge + toast.
4. ✅ **Firestore rules hardened** — `isCompanyOwner()` helper, staff create/update restricted to owner or super_admin, self-edit limited to schedule/display fields.
5. ✅ **Login page integration** — `claimInvite` called at login when no `companyId` claim found (fixed chicken-and-egg problem where dashboard gate blocked callable).
6. ✅ **`useCompany` expanded** — Fallback query using `companyIds` from claims for non-owner staff.
7. ✅ **Firestore index** — Added `COLLECTION_GROUP` composite index for `staff` (email + status).

**Architecture decision:**

- Dropped `beforeUserCreated` blocking function — not supported in Firebase Auth emulator. `claimInvite` callable is the sole auto-link mechanism, called at login time.

**Emulator verification:**

- ✅ Staff invite → login → claimInvite → RBAC-gated dashboard (test user sees only Dashboard/Establishments/Bookings)
- ✅ Kill switch → status `removed` → "Access Denied" on next login
- ✅ `onStaffStatusChange` trigger fires and early-returns correctly on no-op updates

**Key files created:**

- `packages/functions/src/staff/claim-invite.ts`
- `packages/functions/src/staff/on-staff-status-change.ts`
- `packages/functions/src/staff/on-auth-create.ts` (created but unused — blocking functions not emulator-compatible)
- `apps/web/business-portal/app/composables/useClaimInvite.ts`

**Key files modified:**

- `packages/functions/src/index.ts` — exported new functions
- `apps/web/business-portal/app/pages/index.vue` — claimInvite at login
- `apps/web/business-portal/app/components/staff/StaffFormSlideover.vue` — suicide prevention
- `apps/web/business-portal/app/composables/useCompany.ts` — claims fallback
- `apps/web/business-portal/app/layouts/dashboard.vue` — claimInvite on mount (backup)
- `firestore.rules` — staff hardening
- `firestore.indexes.json` — collection group index

---

### 2026-02-11 (Staff Management — Phase 5A+5B: Shift Schemas + Schedule Editor)

**What was accomplished:**

1. ✅ **5A: Shift Schemas** — Created `shift.ts` in shared-types: `ShiftBlock` (start/end/label), `DayShift` (isWorking + blocks array), `WeeklyShift` (7-day pattern), `DateOverride` (off/sick/custom with blocks). Multi-block design enables break times.
2. ✅ **5A: Staff schema migration** — Replaced `schedule: OpeningScheduleSchema` with `weeklyShifts: WeeklyShiftSchema` + `dateOverrides: DateOverrideSchema[]`
3. ✅ **5A: Tests** — 17 new shift schema tests + updated staff tests = 65/65 passing
4. ✅ **5B: useStaffSchedule composable** — CRUD for weekly shifts + date overrides, `getEffectiveSchedule` resolver (overrides take precedence over weekly)
5. ✅ **5B: WeeklyShiftEditor.vue** — Visual 7-day editor with per-day toggle, multi-block time pickers (15-min increments), add/remove blocks, total hours counter
6. ✅ **5B: DateOverrideManager.vue** — Override CRUD with type selection (off/sick/custom), reason field, custom time blocks, sorted list with delete
7. ✅ **5B: Wired to StaffFormSlideover** — Schedule section appears in edit mode when `isBookable` is on, with dedicated "Save Schedule" button
8. ✅ **Phase 4 cleanup** — Fixed booking filter bug (USelectMenu object emission), removed disabled "New Booking" button

**Key files created:**

- `packages/shared-types/src/shift.ts` — Shift schemas
- `packages/shared-types/tests/shift.test.ts` — 17 tests
- `apps/web/business-portal/app/composables/useStaffSchedule.ts` — Schedule CRUD
- `apps/web/business-portal/app/components/schedule/WeeklyShiftEditor.vue`
- `apps/web/business-portal/app/components/schedule/DateOverrideManager.vue`

**Key files modified:**

- `packages/shared-types/src/staff.ts` — `schedule` → `weeklyShifts` + `dateOverrides`
- `packages/shared-types/tests/staff.test.ts` — Updated schedule test
- `apps/web/business-portal/app/components/staff/StaffFormSlideover.vue` — Added schedule section
- `apps/web/business-portal/app/pages/bookings/index.vue` — Filter fix + removed disabled button

---

### 2026-02-11 (Staff Management — Phase 4: Bookings Detail View)

**What was accomplished:**

1. ✅ **BookingDetailSlideover** — Interactive slideover with Customer, Schedule, Payment, Staff Assignment (dropdown from `useStaff`), editable Notes, Service Items, Metadata sections
2. ✅ **useBookings extended** — Added `updateBookingStatus`, `assignStaff`, `updateNotes` action methods + client-side filtering (status, store, staff)
3. ✅ **Bookings page wired** — Click handler on cards opens slideover, filter toolbar (status + store dropdowns) added below stats bar, empty-filter-results state
4. ✅ **RBAC-gated actions** — Confirm/Cancel/Complete/No-Show buttons in slideover footer, only shown with `can_manage_bookings`

**Key files created:**

- `apps/web/business-portal/app/components/bookings/BookingDetailSlideover.vue`

**Key files modified:**

- `apps/web/business-portal/app/composables/useBookings.ts` — Action methods + filtering
- `apps/web/business-portal/app/pages/bookings/index.vue` — Click handler + filter toolbar

---

### 2026-02-11 (Staff Management — Phases 1 through 3)

**What was accomplished:**

1. ✅ **Phase 1 — Schema & Types** — Created `StaffMember`, `CreateStaffMember`, `UpdateStaffMember` Zod schemas in `shared-types`. 9 capabilities, 4 statuses, `storeCapabilities` override map. 22 tests passing.
2. ✅ **Phase 2 — CRUD + List UI** — Built `useStaff.ts` composable (reactive Firestore binding, add/update/soft-delete, search/filter/stats). Built `StaffFormSlideover.vue` (invite/edit form with grouped caps, store multi-select, avatar picker). Rebuilt `staff/index.vue` (search, status tabs, store filter, loading/empty states). Added Firestore security rules for staff subcollection.
3. ✅ **Phase 3 — RBAC Enforcement (Frontend)** — Created `useStaffPermissions.ts` composable (reactive staff doc query, `hasCapability(cap, storeId?)`, owner bypass `company.ownerId === uid`, resolution: owner → storeCapabilities → defaultCapabilities). Wired capability gates into sidebar nav, services, media, staff, and bookings pages.

**Tests:** 48/48 passing (22 staff + 26 rbac), zero regressions.

---

### 2026-02-10 (Phase 4 — Polish & Edge Cases)

**What was accomplished:**

1. ✅ **Hold Expiration Handling** — When countdown hits 00:00, auto-resets to time selection, shows amber warning banner, sends toast
2. ✅ **Network Error Handling** — `useBooking.ts` now distinguishes network vs business logic errors; slot error state has retry button
3. ✅ **Confirm Button Loading** — New `confirming` prop threaded through BookingSlideover → StandardBookingFlow, button shows spinner + disabled during createHold + createBooking
4. ✅ **Empty States** — "No services" message improved for both public and preview modes
5. ✅ **Hold countdown on confirm step** — Visible timer with color shift (amber → red at <2min)

**Key files modified:**

- `packages/ui/components/booking/flows/StandardBookingFlow.vue`
- `packages/ui/components/booking/BookingSlideover.vue`
- `public-marketplace/app/pages/[category]/[slug].vue`
- `public-marketplace/app/composables/useBooking.ts`
- `packages/ui/components/establishment/EstablishmentPage.vue`

---

### 2026-02-10 (Phase 3 — User Profile Bookings)

**What was accomplished:**

1. ✅ **Profile Index Crash Fix** — `useBookings()` composable didn't exist, replaced with `useUserBookings()`
2. ✅ **BookingCard Component** — New reusable card inspired by BookingConfirmation design
3. ✅ **Profile Overview Redesign** — Killed filler stats + Quick Actions, replaced with hero card + calendar with UChip booking dots
4. ✅ **Cancel Booking Flow** — UModal confirmation with overlay, `cancelBooking()` in composable, toast feedback
5. ⚠️ **Cancel blocked by Firestore rules** — `PERMISSION_DENIED: No matching allow statements` — needs rules update or Cloud Function

---

### 2026-02-09 (The Big Fix Session)

**What was accomplished:**

1. ✅ **CORS Fix** — Added port 3002 to MercuryEngine CORS allowlist
2. ✅ **Emulator Region Fix** — `getFunctions(app, 'europe-west1')` is a different singleton from `getFunctions(app)`. nuxt-vuefire connected emulator to wrong instance. Manual connection now targets the correct regional instance
3. ✅ **SSR Fix** — Guard `useCallableFunctions` against server-side rendering (`import.meta.server`)
4. ✅ **Playground Upgraded** — Cascading Company/Store dropdowns, correct region, SSR-safe
5. ✅ **Bookings Admin Page** — New page with Company→Store→Status filters, search, UTable, detail slideover
6. ✅ **Bookings API** — New `server/api/bookings/bookings.get.ts` with filtering, pagination, name resolution

**Critical Learning:**

> Firebase's `getFunctions(app)` and `getFunctions(app, 'europe-west1')` return **completely different singleton instances**. If you connect the emulator to one, the other still points at production. This was the root cause of months of CORS errors.

---

### 2026-02-15 (Cloud Run Deployment + Cross-Domain Maintenance Bypass)

**What was accomplished:**

1. ✅ **Cloud Functions deployed** — All 19 to `europe-west1`. 9 stale `us-central1` ghosts deleted. `auth_onUserCreated` converted from GCIP to v1 `auth.user().onCreate()`.
2. ✅ **Dockerfile.web + deploy.sh** — Multi-stage Docker build + Artifact Registry (`dittodatto-web`) + deploy script.
3. ✅ **All 3 apps deployed to Cloud Run** — Redeployed to `europe-west4` with original service names (`public-marketplace`, `business-portal`, `admin-panel`). Old `dd-*` services in `europe-west1` deleted.
4. ✅ **Firebase Auth domains added** — 3 Cloud Run URLs added to authorized domains. All 3 apps login-verified.
5. ✅ **Cross-domain maintenance bypass** — `dd_bypass` cookie on `.dittodatto.no` set on login (admin/portal), checked server-side by marketplace maintenance middleware.

**Key files:** `Dockerfile.web`, `.dockerignore`, `deploy.sh`, `packages/ui/utils/bypass-cookie.ts`

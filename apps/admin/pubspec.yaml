name: ditto_admin
description: DittoDatto Admin Panel — platform administration interface.
publish_to: 'none'
version: 0.1.0+1

resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  ditto_design:
    path: ../../packages/ditto_design
  mercury_client:
    path: ../../packages/mercury_client
  flutter_riverpod: ^3.3.1
  go_router: ^17.2.3
  google_fonts: ^8.1.0
  surrealdb: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
